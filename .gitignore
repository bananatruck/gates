.cache/
.venv/
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
gate_artifacts/

# libreoffice lock files
.~lock.*#

# Gate 3 diagrams and their exports. Kept out of the repo by decision, so a
# stray `git add -A` cannot pick them up again.
docs/gate3/

# A generated Sep 7 status bundle that 85af14c swept in. Kept on disk, no
# longer tracked; the signed package beside it is the report of record.
reports/status.zip

# Never commit a credential file, wherever it is copied from.
*.env

# The draft's build output. `tectonic paper/draft/main.tex` rebuilds it.
paper/draft/main.pdf
